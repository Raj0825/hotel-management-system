CREATE DATABASE hotel_db;
SELECT USER();
ALTER USER 'root'@'localhost' IDENTIFIED BY 'root1';

SHOW DATABASES;
USE hotel_db;
SELECT SCHEMA_NAME
FROM INFORMATION_SCHEMA.SCHEMATA
WHERE SCHEMA_NAME = 'hotel_db';

SELECT * FROM guests;
